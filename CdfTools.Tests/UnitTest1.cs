using System;
using Xunit;
using CdfTools;
namespace CdfTools.Tests
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
           /* var sv = new CdfTools.SchematronValidator();
            var result = CdfTools.SchematronValidator.Schematron();
            var nodes = result.SelectNodes("/svrl:schematron-output/svrl:failed-assert");
            foreach(var node in nodes){
             //   nodes.
            }
            Console.Write(result.ToString());
            // Assert.False(true);
            */
        }
    }
}
